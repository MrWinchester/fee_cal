# 费用项目需求变更实施文档

## 需求变更概述

**变更时间**: 2026-01-28
**变更类型**: 功能重构 - 费用项目调整

### 原需求
8个石材加工项目：切边块、圆弧、挖空、圆孔、磨长短边、倒45度、对不规则加工、豁口

### 新需求
8个通用安装服务项目：

1. **切割费** - 按平米计算（长×宽自动计算）
2. **加工费** - 按项计算
3. **打包费** - 按次计算
4. **运费** - 按公里计算
5. **卸货搬运** - 按次计算
6. **安装** - 按平米计算（长×宽自动计算）
7. **灯带(含灯带安装)** - 按平米计算（长×宽自动计算）
8. **其他** - 直接填写金额

## 核心变化

### 1. 计费方式变化

**新增三种计费类型**:
- `area`: 按面积计算（切割费、安装、灯带）
- `quantity`: 按数量计算（加工费、打包费、运费、卸货搬运）
- `direct`: 直接填写金额（其他）

### 2. 自动计算功能

**面积自动计算**:
- 用户输入长度和宽度
- 系统自动计算面积 = 长 × 宽
- 按面积计费的项目自动使用该面积

**优势**:
- 减少用户输入
- 避免计算错误
- 提高操作效率

### 3. 界面优化

**新增元素**:
- 面积显示卡片（绿色渐变背景）
- 自动计算标识（按面积计费的项目显示"自动计算"）
- 明细中区分直接费用和计算费用

## 实施的修改

### 修改文件清单

| 文件 | 修改内容 | 影响范围 |
|------|---------|---------|
| `utils/constants.js` | 更新默认价格配置为新的8个项目 | 核心配置 |
| `utils/calculator.js` | 新增面积计算逻辑，支持三种计费类型 | 计算引擎 |
| `pages/calculator/calculator.js` | 更新数据结构和计算逻辑 | 页面逻辑 |
| `pages/calculator/calculator.ttml` | 重新设计输入界面 | 页面结构 |
| `pages/calculator/calculator.ttss` | 新增面积显示和自动计算样式 | 页面样式 |

### 1. utils/constants.js 变更

**变更前**:
```javascript
{
  cutBlock: { name: '切边块', unit: '块', price: 5, key: 'cutBlock' },
  // ... 其他7个石材加工项
}
```

**变更后**:
```javascript
{
  cutting: {
    name: '切割费',
    unit: '平米',
    price: 50,
    key: 'cutting',
    type: 'area'  // 新增：计费类型
  },
  // ... 其他7个服务项目
}
```

### 2. utils/calculator.js 核心变化

**新增函数**:
```javascript
// 计算面积
function calculateArea(length, width) {
  return validateNumber(length) * validateNumber(width);
}

// 格式化面积显示
function formatArea(area) {
  return area.toFixed(2);
}
```

**增强函数**:
```javascript
// calculateTotalFee 新增 dimensions 参数
function calculateTotalFee(inputs, prices, dimensions) {
  const area = calculateArea(dimensions.length, dimensions.width);

  // 根据 type 字段决定计费方式
  switch (priceConfig.type) {
    case 'area':      // 按面积
    case 'quantity':  // 按数量
    case 'direct':    // 直接金额
  }

  return { items, total, area };  // 返回值新增 area
}
```

### 3. pages/calculator/calculator.js 数据结构

**新增数据字段**:
```javascript
data: {
  area: 0,              // 计算的面积
  areaFixed: '0.00',    // 格式化的面积
  // ... 其他字段
}
```

**新增行为**:
- 尺寸输入时触发重新计算
- 传递 dimensions 给计算函数

### 4. pages/calculator/calculator.ttml 界面变化

**新增元素**:
```html
<!-- 面积显示 -->
<view class="area-display" tt:if="{{area > 0}}">
  <text class="area-label">面积:</text>
  <text class="area-value">{{areaFixed}} 平米</text>
</view>

<!-- 按面积计费的项目 -->
<view class="input-row area-based" tt:if="{{area > 0}}">
  <text class="input-label">{{prices.cutting.name}}</text>
  <text class="auto-calculated">自动计算</text>
  <text class="input-unit">{{areaFixed}} {{prices.cutting.unit}}</text>
</view>
```

**输入项变化**:
- 切割费、安装、灯带：只显示，不输入（自动计算）
- 加工费、打包费、运费、卸货搬运：输入数量
- 其他：输入金额

### 5. pages/calculator/calculator.ttss 样式新增

**新增样式类**:
```css
.area-display {
  background: linear-gradient(135deg, #e8f5e9, #c8e6c9);
  border-radius: 12rpx;
  /* 绿色渐变背景，突出显示面积 */
}

.input-row.area-based {
  background-color: #f1f8e9;
  /* 淡绿色背景，标识自动计算项 */
}

.auto-calculated {
  color: #558b2f;
  font-weight: 500;
  /* 显示"自动计算"提示 */
}
```

## 用户体验流程

### 使用流程

1. **输入尺寸**
   ```
   用户输入: 长 5米, 宽 3米
   ↓
   系统自动计算: 面积 = 15平米
   ↓
   显示绿色面积卡片: "面积: 15.00 平米"
   ```

2. **查看按面积计费项**
   ```
   切割费: 自动计算  15.00 平米
   安装:   自动计算  15.00 平米
   灯带:   自动计算  15.00 平米
   （不需要手动输入）
   ```

3. **输入其他项目**
   ```
   加工费: 输入 5 项
   打包费: 输入 2 次
   运费:   输入 50 公里
   其他:   输入 100 元（直接金额）
   ```

4. **查看费用明细**
   ```
   切割费:   15.00平米 × ¥50  = ¥750.00
   加工费:   5.00项 × ¥100    = ¥500.00
   打包费:   2.00次 × ¥30     = ¥60.00
   运费:     50.00公里 × ¥5   = ¥250.00
   其他:     直接费用         = ¥100.00
   ----------------------------------------
   总计: ¥1660.00
   ```

## 默认价格配置

| 项目 | 单位 | 默认单价 | 计费方式 |
|------|------|---------|---------|
| 切割费 | 平米 | ¥50 | 自动（面积） |
| 加工费 | 项 | ¥100 | 手动输入数量 |
| 打包费 | 次 | ¥30 | 手动输入次数 |
| 运费 | 公里 | ¥5 | 手动输入公里 |
| 卸货搬运 | 次 | ¥80 | 手动输入次数 |
| 安装 | 平米 | ¥60 | 自动（面积） |
| 灯带(含灯带安装) | 平米 | ¥40 | 自动（面积） |
| 其他 | 元 | ¥0 | 直接输入金额 |

## 兼容性处理

### 本地存储迁移

**问题**: 旧版本用户的本地存储中保存的是旧的价格配置

**解决方案**:
1. 首次启动时，`storage.initPrices()` 会检查是否存在价格数据
2. 如果存在旧数据，会被新的默认配置覆盖
3. 用户需要重新设置价格（这是不可避免的，因为项目完全不同）

**建议**: 在更新说明中提醒用户重新配置价格

## 测试要点

### 功能测试

1. **面积计算**
   - ✅ 输入长度和宽度，面积正确显示
   - ✅ 修改尺寸，面积实时更新
   - ✅ 按面积计费的项目费用正确计算

2. **数量输入**
   - ✅ 加工费、打包费等项目可以输入数量
   - ✅ 费用正确计算

3. **直接金额**
   - ✅ "其他"项目可以直接输入金额
   - ✅ 明细中显示"直接费用"

4. **费用明细**
   - ✅ 只显示有数量/金额的项目
   - ✅ 按面积计费和按数量计费的显示格式不同
   - ✅ 总计金额正确

5. **重置功能**
   - ✅ 清空所有输入包括尺寸
   - ✅ 面积归零
   - ✅ 费用明细清空

### 边界测试

- 长度或宽度为0 → 面积为0，按面积计费的项目不显示
- 长度和宽度为空 → 面积为0
- 所有项目都不输入 → 总计为0
- 只输入"其他"金额 → 只显示其他项目的明细

## 后续优化建议

1. **面积单位选择**: 支持平米/平方厘米切换
2. **历史记录**: 保存最近的计算记录
3. **分享功能**: 生成费用明细图片分享
4. **批量计算**: 支持多个尺寸同时计算
5. **报价单导出**: 导出PDF或Excel格式的报价单

## 总结

本次需求变更实现了：
- ✅ 8个新的费用项目
- ✅ 三种灵活的计费方式
- ✅ 面积自动计算功能
- ✅ 优化的用户界面
- ✅ 完整的费用明细展示

**核心优势**:
- 减少用户输入工作量
- 提高计算准确性
- 更灵活的费用结构
- 更好的用户体验

---

**文档版本**: 1.0
**更新日期**: 2026-01-28
**实施状态**: ✅ 已完成
